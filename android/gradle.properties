# Sized for a 14GB machine that also runs Flutter + an editor. The previous
# -Xmx4G/-XX:MaxMetaspaceSize=2G gave the daemon a 6GB ceiling and OOM-killed
# the box during a release build. 3G is ample for this app, R8 included.
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.daemon=false
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
